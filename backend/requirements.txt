# ── 核心框架 ──
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
aiofiles>=23.0.0
httpx>=0.27.0

# ── 向量数据库 ──
chromadb>=0.5.0

# ── 文本处理 ──
langchain-text-splitters>=0.2.0

# ── ML 模型推理 ──
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.30.0

# ── 模型下载工具 ──
huggingface_hub>=0.23.0
